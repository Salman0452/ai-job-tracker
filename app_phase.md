Phase 1 — Backend Foundation
  Models + migrations (all 5 tables)
  Auth routes (register, login, /me)
  Role-based dependency (get_current_user, require_admin)

Phase 2 — Resume Module
  Upload endpoint + PDF text extraction
  List, delete, set primary

Phase 3 — Applications Module
  CRUD for applications
  Filtering by status

Phase 4 — AI Pipeline
  ai_service.py with all 4 Groq calls
  Background task trigger
  Polling endpoint

Phase 5 — Analytics + Admin
  Analytics aggregation queries
  Admin user management
  Admin logs

Phase 6 — Frontend
  Landing page
  Auth pages
  Dashboard layout + sidebar
  Applications list + detail
  New application flow
  Resume manager
  Analytics page
  Admin panel

Phase 7 — Polish
  Loading states, error handling
  Empty states
  Mobile responsiveness